---
name: proposal
description: Generate a complete 9-section Technical Proposal and register it in `.sdlc/state.json`.
allowed_tools: ["Read", "Write", "Grep", "Glob", "Shell"]
---

# /proposal — Technical Proposal (Proposal Phase)

This command is defined in `commands/proposal.md` in the repo.

If you are reading this from `.claude/commands/`, it exists to make `/proposal`
discoverable as a project command without requiring users to manually copy files
into `~/.claude/commands/`.

Open `commands/proposal.md` for the full workflow steps.
