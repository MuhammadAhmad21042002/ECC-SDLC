---
name: sdlc-status
description: Display current pipeline phase, artifact statuses with hashes, requirement counts by type, compliance flags, and traceability coverage. Read-only dashboard — no writes to state.json.
allowed_tools: ["Read", "Shell"]
---

# /sdlc-status

This command is defined in `commands/sdlc-status.md` in the repo.

If you are reading this from `.claude/commands/`, it exists to make `/sdlc-status`
discoverable as a project command without requiring users to manually copy files
into `~/.claude/commands/`.

Open `commands/sdlc-status.md` for the full workflow steps.

