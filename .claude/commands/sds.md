---
name: sds
description: Generate SDS (Software Design Specification) and register it in `.sdlc/state.json`.
allowed_tools: ["Read", "Write", "Grep", "Glob", "Shell"]
---

# /sds — Software Design Specification (Design Phase)

This command is defined in `commands/sds.md` in the repo.

If you are reading this from `.claude/commands/`, it exists to make `/sds`
discoverable as a project command without requiring users to manually copy files
into `~/.claude/commands/`.

Open `commands/sds.md` for the full workflow steps.

