---
name: srs
description: Generate SRS (Software Requirements Specification) and register it in `.sdlc/state.json`.
allowed_tools: ["Read", "Write", "Grep", "Glob", "Shell"]
---

# /srs — Software Requirements Specification (Requirements Phase)

This command is defined in `commands/srs.md` in the repo.

If you are reading this from `.claude/commands/`, it exists to make `/srs`
discoverable as a project command without requiring users to manually copy files
into `~/.claude/commands/`.

Open `commands/srs.md` for the full workflow steps.

