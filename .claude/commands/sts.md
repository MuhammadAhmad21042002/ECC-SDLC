---
name: sts
description: Generate STS (Software Test Specification) and register it in `.sdlc/state.json`.
allowed_tools: ["Read", "Write", "Grep", "Glob", "Shell"]
---

# /sts — Software Test Specification (Test Planning Phase)

This command is defined in `commands/sts.md` in the repo.

If you are reading this from `.claude/commands/`, it exists to make `/sts`
discoverable as a project command without requiring users to manually copy files
into `~/.claude/commands/`.

Open `commands/sts.md` for the full workflow steps.
